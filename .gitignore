node_modules
web/dist
*.tsbuildinfo
*.db
*.db-journal
.env
.env.*
.hive/
.DS_Store
.serena/
*.key
*.crt

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# per-task scratch evidence, attached through the hive API, never committed
evidence-*/
