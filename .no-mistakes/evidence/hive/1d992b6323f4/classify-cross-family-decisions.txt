$ bun run classify_evidence.ts   # cross-family data-text probe (Task #523)

CASE                                      COMMAND                                                   DECISION
------------------------------------------------------------------------------------------------------------
quoted SQL DROP/TRUNCATE in commit msg    git commit -m "add DROP TABLE migration and TRUNCATE...   UNKNOWN
quoted kill/pkill in commit msg           git commit -m "guard against kill/pkill of the human...   UNKNOWN
quoted git push --force in commit msg     git commit -m "reject git push --force to shared refs"    UNKNOWN
quoted chmod 777 in commit msg            git commit -m "note: chmod 777 is world-writable, wa...   UNKNOWN
mkfs/dd in hive emit note                 hive emit abc123 status --note "the mkfs/dd path is ...   SAFE
force-delete branch in echoed string      echo "to force-delete a branch run git branch -D name"    SAFE
REAL sqlite3 DROP TABLE                   sqlite3 db 'DROP TABLE t'                                 DANGEROUS
REAL git push --force                     git push --force origin main                              DANGEROUS
REAL chmod -R 777 /                       chmod -R 777 /                                            DANGEROUS

# Quoted danger keywords -> UNKNOWN/SAFE (never DANGEROUS): stays data, auto-approve not blocked incorrectly.
# Real unquoted commands  -> DANGEROUS: safety boundary intact.

$ bun test server/test/classify.test.ts

 21 pass
 0 fail
 236 expect() calls
Ran 21 tests across 1 file. [24.00ms]
