Agent brief composed for a task in each project (GET /api/tasks/:id/brief).
Both projects received the identical command sequence from 01-cli-before-after.txt
and 04-api-response-matrix.txt.

=============== BEFORE (base 4661487) ===============
Every routine watcher summary is pinned into the brief as a past regression:

## Project knowledge (search it — DON'T ask the director for what's here)
This project has stored references, past-failure learnings, and policies. Before
you assume, guess, or ask the director something a teammate would already know,
search them:

  hive recall <keywords>          # e.g. hive recall figma design, hive recall migration

Reference facts on file (run `hive recall` for the full detail of any):
- **routine summary**
- **durable fact**

## Known failure patterns (learn from past regressions)
### real regression (seen 1×)

### routine summary (seen 1×)

### routine summary (seen 1×)

### misfiled routine summary (seen 1×)

### feedback-doc watcher cycle 31: 2 items triaged (seen 1×)
both items already tracked

### feedback-doc watcher cycle 27: no new items (seen 1×)
routine triage summary, nothing to act on

and the brief's own instruction to agents omitted --kind, which is what taught
the watcher to omit it:
  hive learning add --project proj_636001a50bf2 --title "one-line pattern" --body "the fix / what to know"
  hive learning add --project proj_636001a50bf2 --kind reference --title "what it is" --body "the fact / link"

=============== AFTER (fix d8dd42f) ===============
Only genuine regressions are pinned as failure patterns; the routine summaries
(including the one corrected after the fact via PUT) sit under reference facts:

## Project knowledge (search it — DON'T ask the director for what's here)
This project has stored references, past-failure learnings, and policies. Before
you assume, guess, or ask the director something a teammate would already know,
search them:

  hive recall <keywords>          # e.g. hive recall figma design, hive recall migration

Reference facts on file (run `hive recall` for the full detail of any):
- **feedback-doc watcher cycle 27: no new items** — routine triage summary, nothing to act on
- **feedback-doc watcher cycle 33: 1 item triaged** — routine triage summary
- **durable fact**

## Known failure patterns (learn from past regressions)
### real regression (seen 1×)

### ingest worker drops rows on retry (seen 1×)
batch cursor not reset

and the brief now tells agents the required flag:
  hive learning add --project proj_1fa455eb560b --kind failure --title "one-line pattern" --body "the fix / what to know"
  hive learning add --project proj_1fa455eb560b --kind reference --title "what it is" --body "the fact / link"
