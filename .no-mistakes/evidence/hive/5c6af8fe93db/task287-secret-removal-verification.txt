# Task #287 evidence: remove committed Tailscale TLS key/cert + gitignore future ones

## 1. Secret files removed from the tree (were committed at repo root in ae3f6d5)
$ git ls-files | grep -i ts.net    # tracked files matching the cert/key
(no results -> both files untracked/removed)

$ ls -1 | grep -i ts.net           # working-tree files at repo root
(no results -> not present on disk)

## 2. No functional change: nothing in the codebase loads these files by path
$ grep -rniI "ts\.net\.\(key\|crt\)" --include=*.{py,js,ts,rs,go,toml,yaml,yml,json,sh} .
(no code references -> removal is a pure git rm, no runtime impact)

## 3. .gitignore now blocks any future *.ts.net.key / *.ts.net.crt from being committed
$ tail -2 .gitignore
*.ts.net.key
*.ts.net.crt

# Simulate the same secret files reappearing at the repo root:
$ git status --porcelain | grep ts.net   # would these show as untracked?
(nothing -> git ignores them, cannot be accidentally re-added)

$ git check-ignore -v davids-macbook-pro.tailaee0e4.ts.net.key davids-macbook-pro.tailaee0e4.ts.net.crt
.gitignore:11:*.ts.net.key	davids-macbook-pro.tailaee0e4.ts.net.key
.gitignore:12:*.ts.net.crt	davids-macbook-pro.tailaee0e4.ts.net.crt
