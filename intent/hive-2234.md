## Problem
## Bug

The board card for corebeat task 60e73ddf1133 (#2216, display CORE-1521) shows the waiting reason "waiting · its intent is still a draft", truncated, while `GET /api/intents` reports its intent int_a85e9e2f6102 as `status: accepted` (accepted_by director). The task itself is `queued` + deferred (deferred_until 9999). So either the badge reads a stale intent status, or the deferred reason is being overridden by the intent-gate text.

## Fix

In the web board card, derive the waiting reason from the current intent status and the task's deferral: a deferred task shows its `deferred` event note (the human blocker), not the intent gate; an accepted intent never yields the "still a draft" text. Make the badge text wrap or expose the full reason on hover/click so it is not cut off. Add a web test with a queued+deferred task whose intent is accepted and assert the badge shows the deferral note.

## Check that must pass

`bun test` for the web card component green with the new case; screenshot of the card for #2216 on the live board after the live checkout updates.

## Proposed outcome
(not stated)

## Affected users and systems
(not stated)

## Constraints
(not stated)

## Open questions
(none)
