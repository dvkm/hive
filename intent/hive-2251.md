## Problem
# Smoke: codex protocol driver end to end

## Why
The codex stream runtime (previous task in this chain) was built against a fake process. Prove it on a real `codex exec --json` before the pane runtime is deleted. Mirror of hive task 2236 (claude protocol smoke, verified 2026-09-16).

## Precondition (director)
`codex exec --json -s read-only "say PING"` must succeed on this host. On 2026-09-17 it failed with "The 'gpt-6-astra' model requires a newer version of Codex" (codex-cli 0.149.1). If it still fails, emit a `blocked` note naming the codex version and stop; do not work around it with a different model.

## Steps
1. Create a throwaway test project via the existing test-project path (grep `config.test === true` in server/src/api.ts and server/src/testProjects.ts) with `agent: "codex"`, `agent_driver: "protocol"`, repo = a scratch git repo under the scratchpad.
2. Create a chore task on it whose brief says: print `env | grep -E '^HIVE_'`, `git branch --show-current`, `codex --version`, write the three outputs to REPORT.md, commit, emit `ready`.
3. Send one steer mid-run through the API (`POST /api/tasks/<id>/send`) asking it to append the word STEERED to REPORT.md, and confirm the resume turn delivered it.
4. Attach as evidence: the task's `spawned` event (must have `tab_id: null`), the transcript jsonl from the runtime log dir, the REPORT.md contents, and `herdr agent list` output showing no pane was created for this task.

## Check that must pass
REPORT.md holds all three outputs plus STEERED, the task reaches in_review with the evidence attached, and no tmux pane or herdr tab exists for the task id.

## Proposed outcome
(not stated)

## Affected users and systems
(not stated)

## Constraints
(not stated)

## Open questions
(none)
